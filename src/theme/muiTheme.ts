import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      // main: "#0b0c12",
      main: "#0e101c",
    },
    secondary: {
      dark: "#606661",
      main: "#fff",
    },
  },
});

export default theme;
